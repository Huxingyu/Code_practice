#include <iostream>
#include <cstdlib>
#include <vector>
#include <set>

using namespace std;

class Solution {
public:
    int singleNumber(vector<int>& nums) {

    }
};

int judge(int num[]){
    int n=sizeof(num);
    set<int,bool> st;
    for(int i=0;i<n;i++){
        st.insert();
    }
}