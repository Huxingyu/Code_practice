#include <stdio.h>
#include <stdlib.h>

int main(){
    char a,b,c;
    a = getchar();
    b = getchar();
    c = getchar();
    putchar(a);
    putchar(b);
    putchar(c);
    // char x[]={};
    // for(int i=0;i<10;i++){
    //     x[i]=getchar();
    // }
    // for(int i=0;i<10;i++){
    //     putchar(x[i]);
    // }
    system("pause");
    return 0;
}